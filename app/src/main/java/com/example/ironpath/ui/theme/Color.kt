package com.example.ironpath.ui.theme

import androidx.compose.ui.graphics.Color

// Kinetic Precision — Surface Hierarchy
val Surface = Color(0xFF0E0E0E)
val SurfaceContainerLowest = Color(0xFF000000)
val SurfaceContainerLow = Color(0xFF131313)
val SurfaceContainer = Color(0xFF1A1A1A)
val SurfaceContainerHigh = Color(0xFF20201F)
val SurfaceContainerHighest = Color(0xFF262626)
val SurfaceBright = Color(0xFF2E2E2E)

// Primary — Electric Green
val Primary = Color(0xFF39FF14)
val PrimaryContainer = Color(0xFF2FF801)
val OnPrimary = Color(0xFF0E0E0E)
val OnPrimaryContainer = Color(0xFFFFFFFF)

// Accent gradient endpoints (for custom gradient buttons)
val GradientStart = Color(0xFF8EFF71)
val GradientEnd = Color(0xFF2FF801)

// On-surface text
val OnSurface = Color(0xFFFFFFFF)
val OnSurfaceVariant = Color(0xFFADAAAa)

// Outline
val Outline = Color(0xFF767575)
val OutlineVariant = Color(0x26FFFFFF) // white at ~15% opacity

// Semantic
val Error = Color(0xFFCF6679)
val OnError = Color(0xFF000000)

// Ambient glow: Primary at 8% opacity (used for elevation effects)
val AmbientGlow = Color(0x1439FF14)
